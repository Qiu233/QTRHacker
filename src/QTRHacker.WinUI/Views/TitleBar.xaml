<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="QTRHacker.Views.TitleBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:QTRHacker.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    xmlns:ic="using:Microsoft.Xaml.Interactions.Core"
    mc:Ignorable="d"
    Height="32">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="32"/>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
            <Viewbox Width="16" Height="16" Stretch="Uniform" VerticalAlignment="Center" HorizontalAlignment="Center">
                <ImageIcon Source="{x:Bind IconSource}"/>
            </Viewbox>
        </Grid>
        <TextBlock Text="{x:Bind Title}" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="4,0,0,0"/>
    </Grid>
</UserControl>
