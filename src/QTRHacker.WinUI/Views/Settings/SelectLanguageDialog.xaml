<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="QTRHacker.Views.Settings.SelectLanguageDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:QTRHacker.Views.Settings"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:loc="using:QTRHacker.Localization"
    xmlns:vm="using:QTRHacker.ViewModels.Settings"
    d:DataContext="{d:DesignInstance Type=vm:LanguageSelectionViewModel, IsDesignTimeCreatable=False}"
    mc:Ignorable="d"
    PrimaryButtonText="{loc:Localization Key=General.Confirm}"
    Title="{loc:Localization Key=Settings.SelectLanguage.Title}">

    <StackPanel Orientation="Vertical" Spacing="20">
        <TextBlock Text="{loc:Localization Key=Settings.SelectLanguage.PopupHelp}"/>
        <ComboBox ItemsSource="{Binding Cultures}" SelectedValue="{Binding SelectedCultureName, Mode=TwoWay}" SelectedValuePath="Name" DisplayMemberPath="NativeName" HorizontalAlignment="Stretch"/>
    </StackPanel>
</ContentDialog>
