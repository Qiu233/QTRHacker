<?xml version="1.0" encoding="utf-8"?>
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:QTRHacker"
    xmlns:local2="using:QTRHacker.Controls">

    <Style TargetType="local2:Cross">
        <Setter Property="AllowFocusOnInteraction" Value="False"/>
        <Setter Property="Foreground" Value="{ThemeResource ApplicationForegroundThemeBrush}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local2:Cross">
                    <Grid Background="Transparent">
                        <Viewbox Stretch="Uniform">
                            <Path Stroke="{TemplateBinding Foreground}" StrokeThickness="1" Data="M 12 0 L 12 24 M 0 12 L 24 12 Z" Width="24" Height="24"/>
                        </Viewbox>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
